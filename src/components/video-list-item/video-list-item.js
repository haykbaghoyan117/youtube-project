import React, { Fragment } from 'react';
import './video-list-item.css'

const VideoListItem = ({ video }) => {
    // const { id, videoId } = video;
//     let _youtubeVideoId = `https://www.youtube.com/embed/${item}`;
//     <div>
//     <iframe src={_youtubeVideoId} title={id} />
// </div>
    return (
        <Fragment>
            hello
            {/* <span>{ id }</span>
            <span>{ videoId }</span> */}
        </Fragment>

    );
};

export default VideoListItem;